console.log('script命令，可以在控制台执行，工程中往往使用代码分离，在标签的src属性中指定要执行的js文件');

let 变量;
const 常量 = "常量";
let number1 = 1;
let number2 = 1.1;
let 空 = null;
let 未定义 = undefined;
let string = "字符串"

// 字符模板
let 模板字符串 = `number is ${number1}`;

const 数组 = new Array(1, 2, 3, 4, 5);
let fruit数组 = ["apple", "苹果", 100, true];

let 对象 = {
    name: "张三",
    age: 20,
    hobbies: ["唱", "跳", "rap", "篮球"]
};

let x = 10;
if (x === 10) {
    console.log('x 绝对等于 10');
}
else {
    console.log('x 不绝对等于 10');
}

